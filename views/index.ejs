<div class="list-group">
    <% books.forEach(function(book) { %>
        <a href="/view/<%= book.id %>" class="list-group-item list-group-item-action">
            <%= book.title %>
        </a>
    <% }); %>
</div>